namespace Reload.Input
{
    public enum InputType
    {
        Action,
        State,
        Range
    }

    public enum ActionType
    {
        Execute,
        Stop
    }

    public enum StateType
    {
        Pressed,
        Released
    }
}
